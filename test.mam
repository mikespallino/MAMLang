k=3
m=5

fibNum = 0
fn fib(n)[
    if (n <= 1) [
        ret n
    ] else [
        c = 0
        n1 = n - 1
        n2 = n - 2
        a = fib(n1)
        b = fib(n2)
        c = a + b
        print(c)
        fibNum = c + fibNum
        ret fibNum
    ]
]


if (k==3) [
    print(k)
    k=k-1
] else [
    print(k)
]

val3 = fib(6)
print(val3)

print(k)
while (k<m) [
    print(k)
    k = k + 1
]

fn fact(num) [
    print(num)
    sum=num
    while (num > 1) [
        num = num - 1
        sum = sum * num
    ]
    ret sum
]

val1 = fact(5)
print(val1)
